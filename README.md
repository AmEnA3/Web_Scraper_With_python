# University of El Oued Web Scraper

A clean, well-organized Python web scraper that extracts the latest activities from the University of El Oued website.

## Features

✓ استخراج العنوان والرابط والملخص لكل نشاط  
✓ تنظيف النصوص (إزالة الأسطر الجديدة والمسافات الزائدة)  
✓ استخدام فاصل `;` في ملف CSV لدعم النصوص العربية  
✓ دعم الصفحات المتعددة (Pagination)  
✓ تعليقات بالعربية توضح كل خطوة  
✓ رسالة نجاح مفصلة عند الانتهاء  

## 📁 الملفات

- `nashatat_scraper.py`: السكربت الرئيسي (requests + BeautifulSoup)
- `requirements.txt`: تبعيات المشروع
- `nashatat_univ_eloued.csv`: ملف الإخراج (يُنشأ تلقائياً بعد التشغيل)

## 🚀 التثبيت والتشغيل

### على Windows PowerShell:

```powershell
# الانتقال إلى مجلد المشروع
cd "C:\Users\Utilisateur\Desktop\Programmtion\Web Scraper"

# تثبيت الحزم المطلوبة
pip install -r requirements.txt

# تشغيل السكربت
python nashatat_scraper.py
```

## 📊 الإخراج

بعد التشغيل بنجاح، ستجد ملف `nashatat_univ_eloued.csv` يحتوي على:

| title | link | summary |
|-------|------|---------|
| عنوان النشاط | رابط المقال | ملخص النشاط |

## ⚙️ التخصيص

يمكنك تعديل هذه المتغيرات في بداية `nashatat_scraper.py`:

```python
# الرابط الأساسي للموقع
BASE_CATEGORY_URL = "https://www.univ-eloued.dz/ar/category/..."

# اسم ملف الإخراج
OUTPUT_CSV = "nashatat_univ_eloued.csv"

# الفاصل في CSV (الافتراضي ';')
CSV_DELIMITER = ';'

# عدد الصفحات (None = جميع الصفحات)
MAX_PAGES = None
```

## 📝 ملاحظات

- السكربت يستخدم استراتيجيات متعددة لاستخراج البيانات، مما يجعله مرناً تجاه تغييرات تصميم الموقع
- التشفير مضبوط على UTF-8 لدعم اللغة العربية
- إذا حدث خطأ في جلب مقال واحد، يستمر السكربت في معالجة باقي المقالات
- يمكن إيقاف السكربت بالضغط على Ctrl+C في أي وقت
